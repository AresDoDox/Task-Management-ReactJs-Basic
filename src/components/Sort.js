import React, { Component } from 'react';

import {
  Button
} from 'reactstrap';

class Sort extends Component {
  render() {
    return (
        <Button color="primary">Sắp xếp</Button>
    );
  }
}

export default Sort;